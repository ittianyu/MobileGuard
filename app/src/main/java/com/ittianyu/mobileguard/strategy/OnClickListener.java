package com.ittianyu.mobileguard.strategy;

import android.content.Context;

/**
 * Created by yu o.
 * the interface of view click listener
 * using the strategy mode
 */

public interface OnClickListener {
    void onSelected(Context context);
}
